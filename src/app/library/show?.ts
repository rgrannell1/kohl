
import {
  KohlProps
} from '../../commons/types.js'

const show = (state:KohlProps) => {
  return {

  }
}

export default Object.assign(show, {
  parameters: 0,
  description: 'show highlight patterns'
})
